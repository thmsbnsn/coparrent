# Archive

Use this folder for retired documents that are still worth keeping for historical context.

Active docs should stay in the topical folders under `project/`, `security/`, `operations/`, or `acquisition/`.
